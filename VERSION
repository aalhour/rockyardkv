# RockyardKV Version Information

## Go Port Version
rockyardkv v0.3.1

## Pinned RocksDB Version
RocksDB v10.7.5
Commit: 812b12bc7
Tag: v10.7.5

## Compatibility
This Go implementation targets on-disk format compatibility with RocksDB v10.7.5.
Use the golden test suite to validate the compatibility claims in `docs/status/compatibility.md`.

## Verification
To verify compatibility:

1. Set `ROCKSDB_PATH` to a RocksDB v10.7.5 checkout.
1. Run the golden test suite.

```bash
export ROCKSDB_PATH=/path/to/rocksdb
cd /path/to/rockyardkv
make test-e2e-golden
```

## Features Not Included (Post v10.7.5)
The following features were added to RocksDB AFTER v10.7.5 and are
intentionally NOT implemented:
- kSubcompactionProgress (resumable compaction)
- SubcompactionProgressPerLevel struct
- SubcompactionProgress struct
